import 'package:flutter/material.dart';

class PersonalAccount extends StatelessWidget {
  const PersonalAccount({super.key});

  @override
  Widget build(BuildContext context) {
    return Center(child: Text('Personal Account Screen'));
  }
}
